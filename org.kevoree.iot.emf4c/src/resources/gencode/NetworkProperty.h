#ifndef H_NetworkProperty
#define H_NetworkProperty
 #include <stdlib.h>
 #include <stdio.h>

typedef struct _NetworkProperty { 
	char * name;
	char * value;
	char * lastCheck;
} NetworkProperty ;

#endif